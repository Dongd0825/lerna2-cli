{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint --ext .js packages/**/*.js --fix"
  },
  "dependencies": {
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.5.1"
  }
}
